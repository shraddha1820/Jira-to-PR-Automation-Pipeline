# Demo Sandbox Repository

This small repository is intentionally simple. The AI workflow modifies it to simulate a bug-fix automation flow.

## Scenario
The `create_user` function currently fails when `email` is missing from the payload. The automation pipeline patches the handler and adds a regression test.
